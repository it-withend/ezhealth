# 🏥 EZ Health - Telegram Health Assistant

Полнофункциональное приложение для отслеживания здоровья, работающее как Telegram Mini App. Красивый зеленый дизайн, AI-консультации, графики, напоминания и многое другое!

## ✨ Основные функции

### ✅ Реализованные функции

1. **Дневник анализов и снимков** - 📄
   - Загрузка и хранение медицинских документов
   - Сортировка по типам и датам
   - Быстрый доступ к результатам анализов

2. **Контроль пульса и сна** - ❤️😴
   - Визуализация данных с графиками (Recharts)
   - Отслеживание пульса, сна, веса, давления, сахара
   - Тренды и рекомендации

3. **Отправка данных доверенным людям** - 👥
   - Добавление контактов через Telegram
   - Управление разрешениями доступа
   - Выбор кому отправлять данные

4. **Уведомления при критических показателях** - 🔔
   - Автоматический мониторинг пульса, давления
   - Отправка уведомлений через Telegram доверенным лицам
   - API для проверки показателей

5. **ИИ-чат для консультаций** - 💬
   - Анализ симптомов и результатов анализов
   - Рекомендации на основе введенных данных
   - Интеграция с Google Gemini API (gemini-2.0-flash)

6. **Генерация краткого пересказа для врача** - 📋
   - Автоматическое создание отчета из диалога
   - Скачивание в текстовом формате
   - Отправка врачу или доверенным лицам

7. **Графики состояния здоровья** - 📊
   - Интерактивные графики (Recharts)
   - Показатели: пульс, сон, вес, давление, сахар
   - Анализ трендов и статистики

8. **Напоминания о приёме лекарств** - 💊
   - Установка времени приема
   - Отметка о факте приема
   - История приема лекарств

9. **Напоминания о привычках** - 🚶💧🌅
   - Питье воды
   - Витамины и добавки
   - Прогулки и физическая активность
   - Кастомные напоминания

## 🎨 Дизайн

- **Основной цвет**: #2D9B8C (насыщенный зеленый)
- **Светлый вариант**: #4db8a8
- **Темный вариант**: #1e6f63
- **Акцент**: #B8E6E0
- **Фон**: белый (#FFFFFF) и светло-серый (#f9f9f9)
- **Адаптивный дизайн** для мобильных устройств
- **Плавные анимации** и переходы

## 🚀 Быстрый старт

### Требования
- Node.js 16+
- npm или yarn

### Frontend установка

```bash
cd frontend
npm install
npm start
```

Приложение откроется на `http://localhost:3000`

### Backend установка

```bash
cd backend
npm install
npm start
```

Backend будет доступен на `http://localhost:3000/api`

### Переменные окружения

**Frontend** (`.env`):
```
REACT_APP_API_URL=http://localhost:3000/api
```

**Backend** (`.env`):
```
PORT=3000
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_BOT_USERNAME=your_bot_username
GEMINI_API_KEY=your_gemini_api_key_here
NODE_ENV=development
```

## 📱 Страницы и маршруты

| Маршрут | Страница | Описание |
|---------|----------|---------|
| `/` | Onboarding | Приветственный экран с таблетками |
| `/home` | Home | Главная с аппоинтментами и близлежащими клиниками |
| `/health` | Health Metrics | Графики и статистика здоровья |
| `/ai-chat` | AI Chat | Чат с виртуальным ассистентом |
| `/reminders` | Reminders | Напоминания о лекарствах и привычках |
| `/documents` | Documents | Дневник анализов и снимков |
| `/generate-report` | Generate Report | Генерация отчета для врача |
| `/profile` | Profile | Профиль и доверенные контакты |

## 🔌 API Endpoints

### Authentication
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход

### User
- `GET /api/user/:id` - Профиль пользователя
- `PUT /api/user/:id` - Обновление профиля

### Health Metrics
- `GET /api/health?userId=X` - Получить метрики
- `POST /api/health` - Добавить метрику
- `DELETE /api/health/:id` - Удалить метрику

### Analysis (Документы)
- `GET /api/analysis?userId=X` - Получить анализы
- `POST /api/analysis` - Добавить анализ
- `DELETE /api/analysis/:id` - Удалить анализ

### Reminders (Напоминания)
- `GET /api/reminders?userId=X` - Получить напоминания
- `POST /api/reminders` - Создать напоминание
- `POST /api/reminders/log` - Отметить прием лекарства
- `DELETE /api/reminders/:id` - Удалить напоминание

### Trusted Contacts
- `GET /api/contacts?userId=X` - Получить контакты
- `POST /api/contacts` - Добавить контакт
- `DELETE /api/contacts/:id` - Удалить контакт

### AI
- `POST /api/ai/analyze` - Анализ сообщения
- `POST /api/ai/generate-report` - Генерация отчета

### Alerts
- `GET /api/alerts?userId=X` - Получить уведомления
- `POST /api/alerts/check` - Проверить показатели

## 📚 Технологии

### Frontend
- **React 18** - UI библиотека
- **React Router v6** - Маршрутизация
- **Recharts** - Графики и диаграммы
- **CSS3** - Стили и анимации
- **Axios** - HTTP запросы

### Backend
- **Node.js / Express** - Сервер
- **SQLite3** - База данных
- **Google Gemini API** - ИИ консультации (gemini-2.0-flash)
- **Telegram Bot API** - Уведомления

## 🔧 Конфигурация

### Database Schema

Приложение использует SQLite с таблицами:

- **users** - Пользователи
- **health_metrics** - Метрики здоровья
- **medical_analyses** - Анализы и снимки
- **trusted_contacts** - Доверенные контакты
- **medications** - Лекарства
- **medication_logs** - История приема лекарств

## 🚀 Деплой

### Render (Backend)
1. Создайте аккаунт на render.com
2. Подключите GitHub репозиторий
3. Создайте Web Service с настройками из `render.yaml`
4. Добавьте переменные окружения в Render

### Netlify (Frontend)
1. Создайте аккаунт на netlify.com
2. Подключите GitHub репозиторий
3. Настройки сборки уже в `netlify.toml`
4. Добавьте `REACT_APP_API_URL` в переменные окружения

### Telegram Mini App
1. Обновите Mini App URL в BotFather
2. Используйте URL деплоя (например, `https://health-tracker.netlify.app`)

## 📖 Документация

Полная инструкция по деплою находится в [DEPLOY.md](./DEPLOY.md)

## 💡 Примеры использования

### Добавить метрику здоровья
```javascript
await fetch('/api/health', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    userId: 1,
    type: 'pulse',
    value: 75,
    unit: 'bpm'
  })
});
```

### Получить анализы пользователя
```javascript
const response = await fetch('/api/analysis?userId=1');
const analyses = await response.json();
```

### Создать напоминание
```javascript
await fetch('/api/reminders', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    userId: 1,
    name: 'Aspirin',
    dosage: '500mg',
    frequency: 'Daily',
    reminderTime: '09:00'
  })
});
```

## 🐛 Решение проблем

### Порт уже используется
```bash
# Linux/Mac
lsof -i :3000
kill -9 <PID>

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### Ошибка подключения к базе данных
- Убедитесь, что `backend/health.db` существует
- Проверьте разрешения на файл
- Удалите БД и перезапустите (она пересоздастся)

### API не отвечает
- Проверьте, что backend запущен
- Убедитесь в правильном URL в `REACT_APP_API_URL`
- Смотрите консоль backend для ошибок

## 📝 Лицензия

MIT License - используйте свободно!

## 👨‍💻 Автор

Разработано как учебный проект для Telegram Mini Apps

---

**Наслаждайтесь здоровым образом жизни! 🏥💪**
