# 🚀 Инструкция по деплою на production сервер

Это руководство поможет вам задеплоить приложение на Render (backend) и Netlify (frontend), а также настроить Telegram Mini App.

## 📋 Содержание

1. [Подготовка к деплою](#подготовка-к-деплою)
2. [Деплой Backend на Render](#деплой-backend-на-render)
3. [Деплой Frontend на Netlify](#деплой-frontend-на-netlify)
4. [Настройка Telegram Mini App](#настройка-telegram-mini-app)
5. [Проверка работы](#проверка-работы)

---

## 🔧 Подготовка к деплою

### 1. Подготовьте репозиторий

Если еще не сделали, создайте Git репозиторий:

```bash
git init
git add .
git commit -m "Initial commit"
```

Загрузите на GitHub/GitLab/Bitbucket.

### 2. Убедитесь, что файлы .env не попали в Git

Проверьте `.gitignore` - там должны быть:
```
backend/.env
frontend/.env
```

---

## 🖥️ Деплой Backend на Render

### Шаг 1: Создание аккаунта

1. Зайдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub (рекомендуется)

### Шаг 2: Создание Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш репозиторий
3. Заполните настройки:

   **Name:** `health-backend` (или любое другое имя)
   
   **Environment:** `Node`
   
   **Build Command:**
   ```bash
   cd backend && npm install
   ```
   
   **Start Command:**
   ```bash
   cd backend && npm start
   ```
   
   **Root Directory:** оставьте пустым (или укажите корень проекта)

### Шаг 3: Настройка переменных окружения

В разделе **"Environment"** добавьте переменные:

```
PORT=10000
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
TELEGRAM_BOT_USERNAME=имя_вашего_бота_без_@
NODE_ENV=production
```

⚠️ **Важно:** 
- `PORT` должен быть `10000` для Render (или используйте переменную `$PORT`)
- `NODE_ENV=production` для production режима
- Токен бота должен быть реальным от BotFather

### Шаг 4: Обновите server.js для Render

Убедитесь, что в `backend/server.js` используется переменная окружения для порта:

```javascript
const PORT = process.env.PORT || 3000;
```

Render автоматически установит `PORT`, так что это должно работать.

### Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнет деплой
3. Дождитесь завершения (обычно 2-5 минут)
4. Скопируйте URL вашего сервиса (например: `https://health-backend.onrender.com`)

### Шаг 6: Проверка Backend

Откройте в браузере:
```
https://ваш-backend-url.onrender.com/api/health-check
```

Должен вернуться JSON: `{"status":"ok","message":"Server is running"}`

---

## 🌐 Деплой Frontend на Netlify

### Шаг 1: Создание аккаунта

1. Зайдите на [netlify.com](https://netlify.com)
2. Зарегистрируйтесь через GitHub (рекомендуется)

### Шаг 2: Подготовка frontend для production

Создайте файл `netlify.toml` в корне проекта:

```toml
[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### Шаг 3: Создание сайта

1. Нажмите **"Add new site"** → **"Import an existing project"**
2. Выберите ваш Git репозиторий
3. Заполните настройки:

   **Base directory:** оставьте пустым
   
   **Build command:** `cd frontend && npm install && npm run build`
   
   **Publish directory:** `frontend/build`

### Шаг 4: Настройка переменных окружения

В разделе **"Site settings"** → **"Environment variables"** добавьте:

```
REACT_APP_API_URL=https://ваш-backend-url.onrender.com/api
```

⚠️ **Важно:** Замените `ваш-backend-url.onrender.com` на реальный URL вашего backend с Render!

### Шаг 5: Деплой

1. Нажмите **"Deploy site"**
2. Netlify начнет сборку и деплой
3. Дождитесь завершения (обычно 2-5 минут)
4. Скопируйте URL вашего сайта (например: `https://health-tracker.netlify.app`)

### Шаг 6: Настройка кастомного домена (опционально)

Если хотите свой домен:
1. В настройках сайта → **"Domain settings"**
2. Добавьте свой домен
3. Настройте DNS записи согласно инструкциям Netlify

---

## 🤖 Настройка Telegram Mini App

### Шаг 1: Обновление Mini App в BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/myapps`
3. Выберите ваше приложение из списка
4. Выберите опцию **"Edit App"** или **"Edit Mini App"**
5. Когда спросит про URL, отправьте URL вашего frontend:
   ```
   https://ваш-frontend-url.netlify.app
   ```
   или
   ```
   https://ваш-домен.com
   ```

### Шаг 2: Настройка кнопки в боте (опционально)

Чтобы добавить кнопку для открытия Mini App в меню бота:

1. Отправьте BotFather команду `/setmenubutton`
2. Выберите вашего бота
3. Отправьте текст кнопки (например: "Открыть приложение")
4. Отправьте URL вашего Mini App:
   ```
   https://ваш-frontend-url.netlify.app
   ```

### Шаг 3: Проверка настроек

1. Откройте вашего бота в Telegram
2. В меню должна появиться кнопка Mini App
3. Нажмите на неё - должно открыться ваше приложение

---

## ✅ Проверка работы

### 1. Проверка Backend

Откройте в браузере:
```
https://ваш-backend-url.onrender.com/api/health-check
```

Должен вернуться: `{"status":"ok","message":"Server is running"}`

### 2. Проверка Frontend

Откройте в браузере:
```
https://ваш-frontend-url.netlify.app
```

Должна открыться страница входа.

### 3. Проверка в Telegram

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку Mini App в меню
3. Приложение должно открыться
4. Авторизация должна пройти автоматически через Telegram

---

## 🔒 Важные моменты безопасности

### Backend (Render)

✅ **Сделано:**
- `NODE_ENV=production` отключает режим разработки
- Проверка Telegram hash работает только с реальными данными
- CORS настроен правильно

⚠️ **Проверьте:**
- Токен бота хранится в переменных окружения (не в коде!)
- База данных SQLite будет храниться на сервере Render

### Frontend (Netlify)

✅ **Сделано:**
- API URL настраивается через переменные окружения
- Приложение работает только с правильным backend

⚠️ **Проверьте:**
- `REACT_APP_API_URL` указывает на ваш backend URL
- HTTPS включен автоматически на Netlify

---

## 🐛 Решение проблем

### Backend не запускается на Render

**Проблема:** Ошибка при деплое

**Решение:**
1. Проверьте логи в Render Dashboard
2. Убедитесь, что `PORT` установлен в переменных окружения
3. Проверьте, что все зависимости указаны в `package.json`
4. Убедитесь, что `NODE_ENV=production`

### Frontend не подключается к Backend

**Проблема:** CORS ошибки или 404

**Решение:**
1. Проверьте `REACT_APP_API_URL` в Netlify (должен быть полный URL с `/api`)
2. Убедитесь, что backend работает (проверьте `/api/health-check`)
3. Проверьте CORS настройки в `backend/server.js`

### Telegram Mini App не открывается

**Проблема:** Ошибка при открытии в Telegram

**Решение:**
1. Проверьте URL в BotFather (должен быть HTTPS)
2. Убедитесь, что frontend доступен по HTTPS
3. Проверьте, что URL правильный (без слеша в конце)
4. Убедитесь, что сайт открывается в обычном браузере

### Авторизация не работает в Telegram

**Проблема:** Ошибка авторизации

**Решение:**
1. Проверьте токен бота в Render (должен быть правильный)
2. Убедитесь, что `NODE_ENV=production` (режим разработки отключен)
3. Проверьте логи backend на Render
4. Убедитесь, что backend URL правильный в `REACT_APP_API_URL`

---

## 📝 Чеклист перед деплоем

- [ ] Backend код загружен в Git репозиторий
- [ ] Frontend код загружен в Git репозиторий
- [ ] `.env` файлы не попали в Git (проверьте `.gitignore`)
- [ ] Backend задеплоен на Render
- [ ] Backend доступен по HTTPS и отвечает на `/api/health-check`
- [ ] Frontend задеплоен на Netlify
- [ ] `REACT_APP_API_URL` в Netlify указывает на backend URL
- [ ] Frontend доступен по HTTPS
- [ ] URL Mini App обновлен в BotFather
- [ ] Mini App открывается в Telegram
- [ ] Авторизация работает в Telegram

---

## 💡 Альтернативные варианты деплоя

### Backend альтернативы:

- **Railway** - аналогично Render, простая настройка
- **Heroku** - классический вариант (требует карту)
- **DigitalOcean App Platform** - хорошая производительность
- **AWS EC2 / Lightsail** - больше контроля, сложнее настройка

### Frontend альтернативы:

- **Vercel** - отличная альтернатива Netlify
- **GitHub Pages** - бесплатно, но требует настройки
- **Cloudflare Pages** - быстрый и бесплатный

### База данных альтернативы:

Для production рекомендуется использовать PostgreSQL вместо SQLite:

- **Render PostgreSQL** - бесплатный план
- **Supabase** - бесплатный PostgreSQL
- **Railway PostgreSQL** - простой в настройке

---

## 📞 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Netlify Documentation](https://docs.netlify.com)
- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [BotFather](https://t.me/BotFather)

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет работать в production на реальных серверах и доступно через Telegram Mini App!

